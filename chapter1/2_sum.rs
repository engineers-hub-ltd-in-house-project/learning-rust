/*
 * 数値計算と結果表示のプログラム
 *
 * このプログラムでは、変数に格納した数値の計算を行い、結果を表示します。
 * 計算結果をフォーマット文字列と共に出力する方法を示しています。
 * println!マクロの{}内に変数が順番に代入されます。
 */

fn main() {
    let x = 10;
    let y = 20;
    let sum = x + y;
    println!("{} + {} = {}", x, y, sum);
}
