/*
 * 変数の宣言と使用
 *
 * このプログラムでは、Rustでの変数宣言方法を示しています。
 * Rustでは `let` キーワードを使って変数を宣言します。
 * 型注釈（:i64）は必須ではなく、多くの場合は型推論されます。
 * 変数は基本的にイミュータブル（不変）ですが、この例ではシンプルな値の宣言と使用を示しています。
 */

fn main() {
    let x = 100;
    let y: i64 = 200;
    let z = x + y;
    println!("{} + {} = {}", x, y, z);
}
